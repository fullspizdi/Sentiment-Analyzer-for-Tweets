```javascript
const { analyzeTweets } = require('./sentimentAnalyzer');

// Define the search term you want to analyze
const searchTerm = '#yoursearchterm';

// Call the function to analyze tweets based on the search term
analyzeTweets(searchTerm);
```
